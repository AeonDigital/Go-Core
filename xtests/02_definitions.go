package xtests

/*
  ARCHITECTURE & SCOPE LIMITATION:
  02_definitions.go acts as the baseline structural blueprint of the package. It houses
  all data contracts (interfaces), stateless schemas (structs), and constant value blocks.

  Design Constraints:
  - Strictly declarative file. No executable logic, methods, or helper functions are allowed.
  - If a struct declared here requires method attachments later, move that struct and its
    associated receivers into a separate dedicated file matching its logical name.
*/

// Insert package interfaces, un-nested structures, and constants below.
